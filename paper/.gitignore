# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.nav
*.out
*.snm
*.synctex.gz
*.toc
*.vrb
*.idx
*.ilg
*.ind
*.bcf
*.run.xml
build/

# Editor side-files
*.swp
.DS_Store

# Build artefacts
main.pdf
main_arxiv.pdf
figures/_build/
tables/*.tex

# But keep the anonymized submission PDF tracked so reviewers (and future agents)
# can verify the build without rebuilding from source.
!main_anonymous.pdf

# main_arxiv.pdf is regenerated via `make arxiv`. Don't track it -- the user
# wanted it as a fresh build artefact each time, not version-controlled.
